node_modules/
dist/
.astro/
.DS_Store

# Export original de Claude Design, solo de referencia local (no entra al sitio).
_diseno-original/

# Variables de entorno (la plantilla vive en .env.example).
.env
.env.local
